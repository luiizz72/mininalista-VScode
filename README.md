
 #VScode-lite
{
  "editor.fontFamily": "JetBrains Mono", 
  // Define a fonte do editor.
  
  "editor.lineHeight": 1.5, 
  // Define o espaçamento vertical entre as linhas.
  
  "editor.tabSize": 2, 
  // Define que o TAB terá 2 espaços.
  
  "workbench.sideBar.location": "right", 
  // Move a barra lateral para a direita.
  
  "workbench.statusBar.visible": false, 
  // Esconde a barra inferior (status bar).
  
  "editor.minimap.enabled": false, 
  // Desativa o minimapa lateral do código.
  
  "editor.hideCursorInOverviewRuler": true, 
  // Esconde o indicador do cursor na régua lateral.
  
  "editor.overviewRulerBorder": false, 
  // Remove a borda da régua lateral.
  
  "editor.scrollbar.horizontal": "hidden", 
  // Esconde a barra de rolagem horizontal.
  
  "editor.scrollbar.vertical": "hidden", 
  // Esconde a barra de rolagem vertical.
  
  "color-highlight.markRuler": false, 
  // Remove marcações de cores na régua lateral.
  
  "breadcrumbs.enabled": false, 
  // Desativa o caminho do arquivo no topo (breadcrumbs).
  
  "workbench.tree.renderIndentGuides": "none", 
  // Remove as linhas de indentação no explorer.
  
  "explorer.openEditors.visible": 1, 
  // Mostra apenas 1 editor aberto na lista do explorer.
  
  "explorer.decorations.colors": false, 
  // Remove cores indicativas de arquivos no explorer.
  
  "editor.bracketPairColorization.enabled": false, 
  // Desativa coloração diferente para cada par de chaves.
  
  "editor.guides.highlightActiveBracketPair": false, 
  // Não destaca o par de chaves ativo.
  
  "editor.guides.bracketPairsHorizontal": false, 
  // Remove linhas horizontais conectando pares de chaves.
  
  "editor.guides.indentation": false, 
  // Remove guias visuais de indentação.
  
  "editor.cursorBlinking": "phase", 
  // Define animação de piscar do cursor.
  
  "editor.cursorSmoothCaretAnimation": "on", 
  // Ativa animação suave do cursor ao mover.
  
  "editor.renderLineHighlight": "none", 
  // Remove destaque da linha atual.
  
  "editor.glyphMargin": false, 
  // Remove a margem esquerda (onde aparecem breakpoints).
  
  "editor.lineNumbers": "off", 
  // Remove numeração das linhas.
  
  "editor.showFoldingControls": "never", 
  // Remove setas para recolher código.
  
  "window.menuBarVisibility": "toggle", 
  // Menu superior só aparece quando pressionar ALT.
  
  "workbench.layoutControl.type": "menu", 
  // Tipo de controle de layout via menu.
  
  "workbench.layoutControl.enabled": false, 
  // Desativa botão de controle de layout.
  
  "window.title": " ", 
  // Remove título da janela.
  
  "workbench.editor.highlightModifiedTabs": false, 
  // Não destaca abas modificadas.
  
  "git.decorations.enabled": false, 
  // Remove indicadores de mudança do Git.
  
  "files.autoSave": "afterDelay", 
  // Ativa auto save após um tempo.
  
  "files.autoSaveDelay": 500, 
  // Define 500ms de atraso para salvar.
  
  "editor.formatOnSave": true, 
  // Formata automaticamente ao salvar.
  
  "workbench.activityBar.location": "hidden", 
  // Esconde barra lateral de ícones (Explorer, Git, etc).
  
  "editor.defaultFormatter": "esbenp.prettier-vscode", 
  // Define Prettier como formatador padrão.
  
  "git.enabled": false, 
  // Desativa o Git no VSCode.
  
  "editor.wordWrap": "on", 
  // Quebra linhas longas automaticamente.
  
  "explorer.confirmDelete": false, 
  // Não pede confirmação ao deletar arquivos.
  
  "explorer.confirmDragAndDrop": false 
  // Não pede confirmação ao arrastar arquivos.

}

